<?xml version="1.0" encoding="utf-8"?>
<!-- Only the folder the app writes files to share or capture into
     (cache/share). It exposed the whole internal files and cache folders. -->
<paths>
    <cache-path name="share" path="share/" />
</paths>
